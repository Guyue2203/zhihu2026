# 此刻彼时：产品与技术方案

## 1. 产品定义

**此刻彼时**是一种基于知乎历史讨论、还原答案形成过程的时间型知识工具。它的出发点是：答案不是固定的，同一个问题在不同的时空会得到不同的解答，那些解答都能回到当时可见的材料上。

副标题：

> 输入一个后来有了答案的问题，查看观点、证据与共识如何随时间转变。

项目选择**知识赛道**。社区内容是证据来源，档案馆和“失败博物馆”是视觉包装，互动只服务于知识理解，不成为游戏本体。

## 2. 用户问题

通用 AI 往往直接给出今天的结论，但会压缩甚至抹掉结论形成的过程。目标用户需要回答：

1. 人们在不同阶段知道什么、相信什么。
2. 哪些新证据改变了答案。
3. 哪些早期少数观点后来被验证或推翻。
4. 每个阶段的判断能否回到原始知乎内容核查。

核心用户包括研究产品、技术和社会议题的人，以及需要撰写论文、回答、商业分析或行业报告的人。

## 3. 产品核心不是事件年表

普通时间线回答“每一年发生了什么”；本产品回答“答案是如何形成的”。每个节点固定包含：

| 层次 | 回答的问题 | 数据来源 |
|---|---|---|
| 阶段 | 当时处于什么时期 | DeepSeek 检索规划，后续由证据校正 |
| 认知 | 当时主要相信什么 | 知乎帖子观点归纳 |
| 新证据 | 什么事实或论据出现 | 知乎原文摘要与链接 |
| 变化 | 相较上一阶段改变了什么 | 有来源约束的 DeepSeek 总结 |
| 局限 | 这组样本不能证明什么 | 检索覆盖和时间语义说明 |

## 4. 路演叙事

示例问题：

> 共享单车为什么失败？

普通 AI 会立刻列出原因。本产品先指出：

> 这个问题在结局出现后很容易回答，但在行业早期，它甚至还不是一个“失败问题”。

随后展开早期乐观、争议出现、风险暴露和当前回看四个阶段。名称“此刻彼时”指向的不是失败结果，而是结论尚未稳定时的知识现场：此刻有此刻的答案，彼时有彼时的答案。

## 5. 当前产品流程

```text
问题输入
→ DeepSeek 生成 3—4 个待验证阶段和阶段查询
→ 本地爬虫发现候选标题、链接和 query hint
→ 知乎官方接口按阶段检索正式帖子（所有搜索按统一节拍依次启动）
→ 来源清洗、热度辅助排序、ContentID 去重
→ DeepSeek 依据正式帖子整理阶段变化
→ 前端展示时间线、证据帖子和局限
```

### 5.1 为什么采用时间线先行

时间线先行把一个宽泛问题拆成多个阶段检索任务，有机会提高时间覆盖和观点差异。但第一次 DeepSeek 输出只能视为**检索假设**，不能直接成为事实。最终阶段名称、认知和变化必须由知乎帖子支持。阶段数量默认约 3—4 个，用户可选择“更少”或“更多”，模型仍按证据完整性自行调整。

### 5.2 为什么保留爬虫

知乎官方接口适合确认帖子，但单次结果有限；本地爬虫适合发现页面里的候选标题和词汇锚点。两者分工为：

| 组件 | 负责 | 不负责 |
|---|---|---|
| DeepSeek 规划 | 拆阶段、生成检索假设 | 证明历史事实 |
| 本地爬虫 | 发现候选标题和 query hint | 作为最终来源 |
| 知乎接口 | 返回正式帖子、作者、摘要和链接 | 判断结论真假 |
| DeepSeek 总结 | 整理阶段变化与观点简介 | 创造帖子 ID 或链接 |

## 6. 当前爬虫算法

### 6.1 输入

```json
{
  "id": "stage-1",
  "period": "早期",
  "cognition": "共享单车被视为低成本出行创新",
  "searchQueries": [
    "共享单车早期为什么受欢迎",
    "共享单车初期用户体验"
  ]
}
```

### 6.2 处理

1. 用第一个 `searchQueries` 请求知乎公开搜索页。
2. 还原 `\\u002F`、转义斜线等 HTML/JSON 内容。
3. 提取问题、回答 URL 和嵌入式 `title` / `name` 字段。
4. 清理 HTML 标签和通用页面标题。
5. 将候选标题与 `cognition + searchQueries` 转成中文二元字组。
6. 按重合数量排序，过滤无关候选。
7. 取最多两个候选标题作为 `queryHint`。
8. 用 `queryHint` 调用知乎官方搜索接口。
9. 爬虫失败、超时或没有候选时退回原始 `searchQueries`。

### 6.3 可观察状态

| 状态 | 含义 | 后续动作 |
|---|---|---|
| `ok` | 找到候选标题 | 使用候选标题检索知乎接口 |
| `empty` | 页面成功但没有候选 | 使用 DS 阶段 query |
| `timeout` | 超过爬虫超时 | 使用 DS 阶段 query |
| `http_error` | 搜索页请求失败 | 使用 DS 阶段 query |
| `fallback_query` | 其他抓取错误 | 使用 DS 阶段 query |
| `disabled` | 配置关闭爬虫 | 使用 DS 阶段 query |

阶段响应同时保留 `crawlerHttpStatus` 和不含隐私的 `crawlerErrorCode`，用于区分网页拒绝、超时和本机网络失败。这些诊断字段只用于展示，不进入 DeepSeek 总结输入；爬虫未成功的阶段由后端在 `limitations` 追加固定降级说明，避免模型把爬虫被拒误读为“无官方来源”。

## 7. 证据与排序规则

### 7.1 来源约束

- 只接受 `https://zhihu.com` 及其子域名链接。
- 页面只展示知乎官方接口返回的帖子。
- DeepSeek 返回的 `postIds` 必须存在于本次帖子集合。
- 模型没有给出有效帖子时，后端按阶段保留热度最高的少量候选。
- `EditTime` 可能是发布时间或编辑时间，不能直接写成事件发生时间。

### 7.2 热度参考

```text
heat = ln(1 + 赞同数) × 10
     + ln(1 + 评论数) × 6
     + RankingScore × 10
     + AuthorityLevel × 2
```

热度用于排序，不代表真实性。最终选择还应考虑阶段相关性、观点差异和来源是否支持结论。

## 8. 前端框架

### 8.1 当前页面

1. 用户输入一个问题或选择示例。
2. 页面进入等待状态。
3. 后端快速返回（命中本地缓存）时仅保留约 1 秒的最短视觉等待；首次生成较慢时持续等待真实结果，期间先显示已进行时间，模型生成的问题过渡文案到达后替换展示。
4. 结果页顶部只保留「提出新问题」返回按钮与问题标题（原副标题论点、eyebrow 标签与指标 chip 已移除；检索口径下移到页面底部的「证据边界与检索说明」）。主体为接近整屏高度的「时间拼图」：各阶段拼图块按数量**等宽均分**整行，按时间先后从左到右排列，默认仅保留时间与 2—3 个关键词：时间标签固定在块顶部，关键词标签在未悬浮时放大、居中、每个标签独占一行（悬浮预览与展开面板里的关键词仍为小号横排）。阶段时长不再决定块宽，改由块上的时间标签体现；时间标签**完整显示起止年份**（如 `2014—2016`，不再缩写成 `2014—16`），阶段较多时按每块可用宽度自动下调字号，不使用省略号截断。
5. 桌面端悬浮或键盘聚焦拼图块时预览阶段认知；点击后该拼图块在原位扩张并承载完整认知、变化依据与知乎来源，其他阶段同步收窄。移动端直接轻触展开，不依赖悬浮。
6. 拼图下方提供跳转知乎创作中心的按钮「未来的答案，由你来写」，链接为 `https://www.zhihu.com/creator/search-question/init?q=<当前问题>`（问题经 URL 编码，新标签页打开），把「看别人的答案」接到「写自己的答案」。
6. 「想了解点什么？」预设 10 个适合观察认知变化的问题，右侧展示当前知乎热搜（官方 `hot_list` 接口，本地缓存 10 分钟）。两栏均同时显示 3 条、每 3 秒向上滚动 1 条（停留 3 秒 + 0.36 秒滚动动画，前端硬编码，不进 `.env`），窄屏时上下堆叠；点击任一条目会填入输入框。两栏只用于发现议题，不参与时间线生成；热搜上游失败时仅隐藏热搜栏。
7. 右上角提供知乎登录入口。未配置 OAuth 时该入口隐藏，主流程完全不受影响；已登录时右上角显示头像与昵称，**点击头像进入用户界面**（`#/me`）。
8. 用户界面分为两块：上方是账号信息卡（头像、昵称、一句话介绍、知乎 UID）与退出登录；下方是三个标签页——**关注的人**、**创作**、**我的检索**。
9. 关注与创作列表使用服务端返回的 `NextOffset` 做「加载更多」，并显示总数；分页信息不完整时明确停止加载而不是自行猜偏移量。点击「我的检索」中的条目会带着当时的阶段偏好与来源模式重新检索。

### 8.2 暂不加入独立澄清步骤

第一版不在输入与生成之间增加一轮强制问答。DeepSeek 已负责将 query 拆成阶段；额外澄清会增加交互成本。只有测试表明宽泛 query 经常导致错误阶段时，才增加一个可选 `scope`：

```json
{
  "query": "元宇宙为什么退潮？",
  "scope": "商业与产业"
}
```

## 9. 稳定契约与可变实现

### 9.1 全队必须对齐

- `POST /api/v1/journey`
- `POST /api/v1/prelude`（等待页过渡文案；失败时前端保留默认等待文案）
- 输入字段 `query`；可选 `stagePreference`（`default` / `fewer` / `more`）、`retrieval`（`zhihu` / `model`）
- `stages[].id` 与 `stages[].postIds`
- `posts[].id`、`question`、`answerer`、`viewpoint`、`url`
- `coverage` 与 `limitations`
- `crawlerStatus`、`crawlerQuery`、`crawlHitCount`
- `GET /api/v1/hot`（首页热搜轮播，可选，失败不影响主流程）
- 密钥只存在后端环境

### 9.2 数据存储与登录契约

- 持久层是项目内 SQLite（Node 内置 `node:sqlite`，零第三方依赖），默认 `.data/zhihu.db`，WAL 模式，`PRAGMA user_version` 管版本，迁移只追加不修改。
- 三张缓存类表：`journey_cache`（键 = query + 阶段偏好 + 来源模式的 sha256）、`hot_cache`（键 = 归并档位 10/20/30）、`user_api_cache`（键 = uid + 接口 + 分页参数，保护每日 100 次的 `user_data` 额度）。
- 三张用户类表：`users`（主键 `uid`，十进制字符串）、`sessions`（`oauth_token` 只存在这里）、`journey_history`（`uid` 外键级联）。
- 缓存读写失败一律静默降级，只回源、不报错；生成失败不写缓存。
- 改造前的 `.cache/*.json` 首次启动自动导入，两表独立判断是否为空，导入后保留原文件。
- 浏览器只持有应用自己的随机 `HttpOnly` 会话标识；app_key、OAuth token、Access Secret 都不进前端。
- `uid` 是 int64，必须无损按字符串传递；前端不得转成 Number。
- 登录是可选能力：未配置 OAuth 时入口隐藏，未登录不写检索历史。

### 9.3 可以独立优化

- DeepSeek 提示词措辞。
- 候选标题解析规则。
- 二元字组相关性算法。
- 每阶段 query 数量。
- 热度权重。
- 页面视觉和动效。

## 10. 评测方法

准备至少 **10 个**固定问题，对“无爬虫”和“启用爬虫”使用同一组 query 测试：

| 指标 | 计算方式 | 目标 |
|---|---|---:|
| 爬虫有效率 | `crawlerStatus=ok` 的阶段 / 总阶段 | 先建立基线 |
| Query 改写率 | 实际 query 与 DS 原 query 不同的阶段 / 总阶段 | 证明爬虫真实参与 |
| 来源有效率 | 可打开且相关的帖子 / 总帖子 | ≥ 90% |
| 阶段可证率 | 有有效帖子的阶段 / 总阶段 | 100% |
| 引用正确率 | 人工核对来源支持描述的阶段 / 总阶段 | ≥ 90% |
| 时间跨度 | 最早与最晚有效内容间隔 | 按题目判断 |
| P95 延迟 | 端到端第 95 百分位 | 路演可接受 |
| 单次成本 | DS 与知乎调用总成本 | 团队预算内 |

只有爬虫显著改善来源有效率、阶段可证率或时间覆盖时，才继续增加解析复杂度。

## 11. 18 天内的实现边界

### 已实现

- 时间线先行。
- 每阶段 1—2 个检索 query。
- 本地 HTML/嵌入式 JSON 候选解析。
- 爬虫失败回退。
- 知乎正式来源确认。
- 生成结果本地缓存与强制刷新。
- 等待页模型过渡文案接口与前端展示。
- DeepSeek JSON 输出校验和帖子 ID 校验。
- 时间线与帖子页面。
- SQLite 持久化：时间线缓存、热搜缓存、用户数据接口缓存、用户、会话与检索历史；改造前 JSON 缓存首次启动自动导入。
- 知乎 OAuth 登录（授权码流程、state 单次校验、服务端会话）、本人检索历史与一键重跑。
- 用户界面：账号信息卡 + 关注的人 / 创作 / 我的检索三个标签页，基于服务端 `NextOffset` 的「加载更多」与总数展示。
- 自动化验证：核心自检 + 持久层/OAuth/用户数据单测（129 项）+ mock 端到端（84 项），全部不依赖外网与真实密钥。

### 暂不实现

- 登录态爬虫或绕过验证码。
- Playwright 浏览器集群。
- 全站抓取。
- 向量数据库。
- 消息队列。
- 关注 / 收藏同步、多用户协作与社交功能。
- OAuth token 刷新（当前协议只返回 `access_token` 与 `expires_in`，无 refresh token，过期需重新授权）。
- 邮箱、手机号等敏感字段的读取与存储。
- 将赞同数直接解释为事实可信度。

## 12. 路演冻结条件

正式展品需要满足：

1. 每个阶段至少有一条可打开的知乎来源。
2. 人工确认来源支持阶段描述。
3. 不把编辑时间误写为事件发生时间。
4. 展示样本限制和检索降级状态。
5. 核心示例保存一份人工审核快照，避免上游失败影响路演。
6. 快照和在线结果使用同一 JSON 契约。

## 13. 权威来源

- [知乎数据开放平台](https://developer.zhihu.com/)
- [知乎搜索 MCP 文档](https://developer.zhihu.com/docs?key=zhihu_search_mcp)
- 知乎热榜接口 `GET /api/v1/content/hot_list`（官方 HTTP API 文档：`hot_list` 章节）
- 知乎 OAuth 授权页 `GET https://openapi.zhihu.com/authorize`、换取 token `POST https://openapi.zhihu.com/access_token`
- 知乎授权用户基础信息 `GET https://openapi.zhihu.com/user`
- Node.js 内置 SQLite：[node:sqlite](https://nodejs.org/api/sqlite.html)
- [DeepSeek Chat Completion API](https://api-docs.deepseek.com/api/create-chat-completion/)
- [DeepSeek JSON Output 指南](https://api-docs.deepseek.com/guides/json_mode/)
